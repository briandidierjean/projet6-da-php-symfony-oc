{% extends 'base.html.twig' %}

{% block title %}Mot de passe oublié | SnowTricks{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {% if errorMsg %}
        <div class="alert alert-danger">{{ errorMsg }}</div>
    {% endif %}
    {{ form_row(form.email) }}

    <button type="submit">Réinitialiser</button>
    {{ form_end(form) }}
{% endblock %}